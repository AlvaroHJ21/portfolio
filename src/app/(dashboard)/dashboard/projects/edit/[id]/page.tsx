import ProjectFormPage from '../../create/page';

export default ProjectFormPage;

// interface Props {
//   params: {
//     id: string;
//   };
// }

// export default function page({ params }: Props) {
//   return <ProjectFormPage id={params.id} />;
// }
